{
  "name": "@factory/agent-worker",
  "version": "0.1.0",
  "type": "module",
  "scripts": { "build": "tsc -p tsconfig.json", "test": "node --test dist/**/*.test.js || true" },
  "dependencies": { "@factory/shared": "workspace:*" }
}
